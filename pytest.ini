[pytest]
# Pytest configuration for SagaApi

pythonpath = src
testpaths = tests

addopts =
    -v
    --strict-markers
    --tb=short
    --cov-report=term-missing
    --cov-report=html

markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
